specVersion: "v1beta"
serviceId: "nesysworks"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "nesysworks-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
